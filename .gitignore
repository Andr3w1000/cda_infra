# Terraform state — never commit state files; use remote state (Azure Blob) for teams
*.tfstate
*.tfstate.*
.terraform/
.terraform.lock.hcl

# Variable files that may contain secrets (subscription IDs, etc.)
*.auto.tfvars
secrets.tfvars

# OS / editor noise
.DS_Store
.idea/
*.swp
